extends ../base

block content 
	div.pageTitle #{pageTitle}
	if errorMessage
		span=errorMessage
	form(method="POST")
		if !loggedInUser.social.socialOnly
			input(placeholder="Password", name="password", type="password", required)
		button.form_submit 탈퇴하기