extends base

block content
	if errorMessage
		span=errorMessage
	div#pageTitle #{pageTitle}
	form(method="POST")
		input(placeholder="Email", name="email", type="email", required)
		input(placeholder="Username", name="username", type="text", required)
		input(placeholder="Password", name="password", type="password", required)
		input(placeholder="Confirm Password", name="password2", type="password", required)
		input(placeholder="Address", type="address", name="address", required)
		input(placeholder="Detailed Address", type="address", name="address2", required)
		input(type="submit", value="Join")